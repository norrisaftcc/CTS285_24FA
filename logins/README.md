# Logins (examples)

A few iterations on the basic problem of logging in.

v1 - plain text (csv) file instead of database.
v2 - basic login form, will let you know if username/pw combo is valid.
v3 - basic sqlite, raw sql no ORM.
v4 - (conjectural) add ORM from something like SQLAlchemy.